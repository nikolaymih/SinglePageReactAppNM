import { Cell, PieChart, Pie } from 'recharts';
import { IUserArray } from '../../interfaces/user.interface';

import './ScoreBoard.css';

export const ScoreBoard = (dataUser: IUserArray) => {
    return (
        <h1>Pie char does not render</h1>
    )
}